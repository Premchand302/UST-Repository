package com;

import static org.junit.Assert.*;

import org.junit.Test;

public class UserStatusTest {

	 @Test
	    public void testUserIsActive() {
	        UserStatus service = new UserStatus();
	        boolean status = service.isUserActive();
	        assertTrue(status);   // active user returns true
	    }

}
